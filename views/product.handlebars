<div class="container">
  <div class="row mb-4 product">
    <div class="col-md-7">
        <p>
          <img width=200 height=200 src="/images/{{filename}}" alt="{{description}}">
        </p>
    </div>
    <div class="col-md-5"> 
      <div class="text-center">
        <h2>{{name}}</h2>
        <p>{{description}}</p>
        <p>
          <span for="img" aria-label="money">💲</span>
          <span class="dollar-amount">{{price}}</span>
        </p>
        <p>
          <a href="/cart" class="button is-primary">Buy</a>
        </p>
      </div>
    </div> 
  </div>
</div>  

